#!/usr/bin/python3
from math import sqrt
print("Input vector values:")
a = float(input("a: "))
b = float(input("b: "))
c = float(input("c: "))

d = sqrt(a**2 + b**2 + c**2)
print("The length of the  length of a given vector", "%.2f" % d )
